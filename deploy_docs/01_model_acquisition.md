# 模型获取

## 模型是yolo11n

## 在NCNN那边找
- 网站：https://github.com/nihui/ncnn-assets/tree/master/models
- 模型：`mobilenetv3_ssdlite_voc`
- 下载文件：
  - `yolo11n.ncnn.param`
  - `yolo11n_cls.ncnn.bin`
- 将文件放入 `model/` 目录，并重命名为 `model.ncnn.param` 和 `model.ncnn.bin`（与代码一致）

## 自己下载 Ultralytics 库转换
（待补充或略）
(这玩意很简单)